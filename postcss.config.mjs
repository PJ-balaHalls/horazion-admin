/** * [FE-HZ-003] Pipeline de processamento de CSS - Tailwind v3.4.1
 * Motor clássico que interpreta o tailwind.config.js e aplica os prefixos de compatibilidade.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;